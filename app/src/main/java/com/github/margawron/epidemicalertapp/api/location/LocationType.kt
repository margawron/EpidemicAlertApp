package com.github.margawron.epidemicalertapp.api.location

import com.github.margawron.epidemicalertapp.R

enum class LocationType(val stringId: Int) {
    INFO(R.string.location_type_info),
    QUARANTINE(R.string.location_type_quarantine);
}